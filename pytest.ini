[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
# -p no:cacheprovider: don't write .pytest_cache — the container runs as a
# non-root user and /app may be a read-only bind mount in CI/dev.
addopts = -ra -p no:cacheprovider